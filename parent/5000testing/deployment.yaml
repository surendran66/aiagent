apiVersion: apps/v1
kind: Deployment
metadata:
  name: 5000testing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 5000testing
  template:
    metadata:
      labels:
        app: 5000testing
    spec:
      containers:
      - name: 5000testing
        image: acrsurenlearning.azurecr.io/5000testing:latest
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "default"
---
apiVersion: v1
kind: Service
metadata:
  name: 5000testing
spec:
  selector:
    app: 5000testing
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
